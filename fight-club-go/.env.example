# Fight Club - Environment Configuration
# Copy this file to .env and fill in your values

# ==========================================
# KICK STREAMING CONFIGURATION
# ==========================================

# Your Kick stream key (required for streaming)
STREAM_KEY_KICK=your_stream_key_here

# Kick OAuth credentials
CLIENT_ID_KICK=your_client_id
CLIENT_SECRET_KICK=your_client_secret
KICK_BROADCASTER_USER_ID=your_broadcaster_id

# Public URL for webhooks (use ngrok or similar for local dev)
PUBLIC_URL=https://your-ngrok-url.ngrok.io

# ==========================================
# VIDEO CONFIGURATION
# ==========================================

# Resolution and framerate (defaults: 1280x720 @ 24 FPS)
# VIDEO_WIDTH=1280
# VIDEO_HEIGHT=720
# VIDEO_FPS=24
# VIDEO_BITRATE=4000

# ==========================================
# HARDWARE ENCODING (NVIDIA)
# ==========================================

# Enable NVIDIA NVENC hardware encoding (requires NVIDIA GPU)
# USE_NVENC=true

# Force NVENC even if test fails (use if you know you have NVENC)
# FORCE_NVENC=true

# ==========================================
# AUDIO CONFIGURATION
# ==========================================

# Background music
# MUSIC_ENABLED=true
# MUSIC_VOLUME=0.15
# MUSIC_PATH=assets/music/digital_fight_arena.ogg

# ==========================================
# IPC MODE - SEPARATED ARCHITECTURE
# ==========================================
# Enable this to run server and streamer as separate processes
# This isolates streaming from server load for smoother frames

# Enable IPC publisher on server (required for separated mode)
# IPC_ENABLED=true

# Unix socket path for IPC communication
# IPC_SOCKET=/tmp/fight-club.sock

# ==========================================
# HOW TO USE SEPARATED MODE
# ==========================================
#
# Option 1: Use the helper script
#   ./scripts/start-separated.sh
#
# Option 2: Manual startup
#   Terminal 1: IPC_ENABLED=true go run ./cmd/server
#   Terminal 2: go run ./cmd/streamer
#
# Benefits of separated mode:
# - Webhook traffic doesn't affect frame rate
# - Server GC pauses don't cause stream stuttering
# - Can run streamer with higher process priority
# - Can run streamer on a different machine (with network socket)
#
# ==========================================

# ==========================================
# SERVER CONFIGURATION
# ==========================================

# Server port (default: 3000)
# PORT=3000

# Admin panel authentication
# ADMIN_AUTH_ENABLED=true

# Event logging
# EVENT_LOG_PATH=events.jsonl

# Disable debug/metrics server
# DISABLE_DEBUG_SERVER=true
