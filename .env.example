# Fight Club - Go Engine Configuration
#
# =============================================================================
# STREAMING ARCHITECTURE - IMPORTANT!
# =============================================================================
# This application uses DIRECT RTMP streaming to Kick - no proxy or tunnel
# is used for video data. This ensures minimal latency and maximum bandwidth.
#
# The PUBLIC_URL (ngrok/tunnel) is ONLY used for:
# - OAuth callback from Kick (authentication)
# - Webhook events from Kick (chat messages, subscriptions, etc.)
#
# Video streaming goes directly: FFmpeg -> Kick RTMP servers
# This bypasses any tunnel and avoids ngrok bandwidth/latency limitations.
# =============================================================================

# Kick Streaming - DIRECT to Kick (no proxy!)
STREAM_KEY_KICK=your_stream_key_here
CLIENT_ID_KICK=your_client_id_here
CLIENT_SECRET_KICK=your_client_secret_here
KICK_BROADCASTER_USER_ID=your_broadcaster_user_id

# OPTIONAL: Custom RTMP endpoint (defaults to Kick's official server)
# RTMP_URL=rtmps://fa723fc1b171.global-contribute.live-video.net:443/app

# Webhook Configuration - ONLY for OAuth/webhooks (NOT for video streaming!)
# This URL is only used to receive HTTP callbacks from Kick's servers.
# Use ngrok, localtunnel, or any public URL that routes to your local server.
PUBLIC_URL=https://your-tunnel-url.ngrok.io

# Redis Cloud (optional)
REDIS_PUBLIC_ENDPOINT=your_redis_endpoint:port
REDIS_PASSWORD=your_redis_password

# MongoDB Atlas (optional)
MONGODB_CLUSTER_USERNAME=your_mongodb_username
MONGODB_CLUSTER_PASSWORD=your_mongodb_password

# Server Config
PORT=3000

# Game Config
MAX_PLAYERS=100
GAME_TICK_RATE=30
STREAM_FPS=30
STREAM_BITRATE=4500

# Event Logging
EVENT_LOG_PATH=events.jsonl

# Debug Server (disable in production)
DISABLE_DEBUG_SERVER=false

# Admin Panel Security (enable in production)
# When enabled, only the broadcaster can access /admin
ADMIN_AUTH_ENABLED=false
